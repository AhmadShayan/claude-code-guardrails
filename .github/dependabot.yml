version: 2
updates:
  # The workflow pins every action to a commit SHA. This keeps those pins current, one
  # reviewable pull request at a time.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ['*']
